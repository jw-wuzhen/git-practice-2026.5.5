# LangChain 命名实体识别与知识图谱系统
## 课程设计报告

**学生：巫俊** | **学号：202411581359** | **日期：2026年5月5日**

---

## 一、系统方法基础说明

### 1.1 系统架构设计

本系统采用四层架构，集成爬虫、Pydantic 模型、NER 引擎和知识图谱：

```
数据流向图：
HTML文件 → 网页爬虫 → Pydantic验证 → 知识图谱存储
             (BeautifulSoup)  (类型检查)   (内存索引)
                                              ↓
                                        命名实体识别
                                              ↓
                                        三元组匹配
                                              ↓
                                        智能答案生成
```

### 1.2 核心数据模型（Pydantic V2）

使用 Pydantic 定义了以下数据模型：

#### (1) 信息框属性模型 (`InfoboxAttribute`)
```python
attribute_name: str    # "出生日期"
attribute_value: str   # "1785年5月26日"
```

#### (2) 百科信息框模型 (`BaikeInfobox`)
```python
entity_name: str              # "林则徐"
entity_type: str              # "人物"
attributes: List[InfoboxAttribute]  # 15个属性
```

#### (3) 知识三元组模型 (`KnowledgeTriple`)
```python
subject: str           # "林则徐"
predicate: str         # "出生日期"
object_value: str      # "1785年5月26日"
confidence: float      # 1.0 (置信度)
```

#### (4) 命名实体模型 (`NamedEntity`)
```python
entity_text: str       # "林则徐"
entity_type: str       # "人物"
confidence: float      # 0.95
position: tuple        # (start, end) 文本位置
```

### 1.3 关键技术方案

| 技术模块 | 实现方案 | 优势 |
|--------|--------|------|
| **网页爬虫** | BeautifulSoup + 正则表达式 | 可靠抽取结构化信息框 |
| **数据模型** | Pydantic V2 ConfigDict | 类型检查、字段验证、JSON Schema |
| **实体识别** | 正则模式 + 关键词匹配 | 快速响应、无外部依赖 |
| **知识存储** | Subject-indexed 字典 | $O(1)$ 查询速度 |
| **答案生成** | 启发式规则引擎 | 准确性强、可解释性高 |

---

## 二、实际落地示例

### 2.1 项目文件结构

```
项目目录/
├── 爬虫.py                    # 网页爬虫（BeautifulSoup）
├── knowledge_graph_models.py  # ✨ Pydantic 数据模型
├── knowledge_graph_system.py  # ✨ 知识图谱查询引擎
├── demo_complete_pipeline.py  # ✨ 完整演示脚本
├── 课程报告.md               # 本报告
├── 林则徐.html               # 爬虫数据源
└── langchain_ie.ipynb        # Jupyter笔记本
```

### 2.2 完整演示（实际运行结果）

运行 `python demo_complete_pipeline.py` 输出：

```
【第一步】网页爬虫抽取信息框数据
📄 源文件: 林则徐.html
✓ 成功抽取 15 条属性

【第二步】转换为知识图谱三元组
✓ 生成 15 个三元组
<林则徐, 全名, 林则徐>
<林则徐, 别名, 林元抚 、 林文忠>
<林则徐, 出生日期, 1785年8月30日>
...

【第三步】Pydantic 模型验证
✓ 模型验证成功
  实体名称: 林则徐
  属性数量: 15
  JSON Schema 验证: 通过 ✓

【第四步】加载到知识图谱系统
✓ 已加载到知识图谱

【第五步】执行智能查询
📝 查询: 林则徐是什么时候出生的？
✓ 识别实体: ['林则徐']
✓ 答案: 林则徐出生于1785年8月30日。
```

### 2.3 命名实体识别机制

系统支持以下实体识别方式：

| 实体类型 | 识别方法 | 示例 | 置信度 |
|---------|--------|------|--------|
| **人物** | 知识库匹配 | 林则徐、康熙 | 0.95 |
| **日期** | 正则表达式 `\d{4}年\d{1,2}月\d{1,2}日` | 1785年5月26日 | 0.95 |
| **属性** | 关键词匹配 | 出生、逝世、籍贯 | 0.85 |
| **地点** | 字典查询 | 福建省、北京 | 0.90 |

### 2.4 查询示例与流程

**用户查询：** "林则徐的籍贯是？"

```python
from knowledge_graph_system import KnowledgeGraphSystem

# 第一步：初始化系统
kg_system = KnowledgeGraphSystem()  # 自动加载数据

# 第二步：处理查询
query = "林则徐的籍贯是？"
result = kg_system.process_query(query)

# 第三步：获取结果
print(result['识别实体'])      # ['林则徐']
print(result['生成答案'])      # "林则徐的全名是林则徐。"
```

**支持的查询类型：**
- 人物基本信息（出生、逝世、籍贯、国籍）
- 人物身份属性（职业、身份、朝代）
- 人物成就荣誉（主要成就、作品、荣誉）
- 综合信息检索

---

## 三、系统特点与扩展

### ✅ 实现特点

1. **完全独立运行** - 无需外部 API（LLM 可选）
2. **类型安全** - Pydantic V2 完全验证
3. **数据追溯** - 从 HTML → 模型 → 知识图谱的完整链路
4. **高效查询** - 基于索引的 O(1) 查询速度
5. **易于扩展** - 支持快速添加新实体和三元组

### 🔧 可扩展方向

1. **LLM 集成** - 集成 GPT/LLaMA 实现智能理解
2. **图数据库** - 迁移到 Neo4j 支持复杂查询
3. **推理引擎** - 实现关系推理（例如：血缘关系）
4. **多源融合** - 支持多个网站的数据融合

---

## 四、运行和使用

### 安装依赖
```bash
pip install pydantic requests beautifulsoup4
```

### 运行方式

**方式1：查看 Pydantic 模型演示**
```bash
python knowledge_graph_models.py
```

**方式2：运行知识图谱系统**
```bash
python knowledge_graph_system.py
```

**方式3：完整数据流程演示（推荐）**
```bash
python demo_complete_pipeline.py
```

---

## 五、设计总结

本项目展示了将**传统网页爬虫**与**现代数据科学工具** (Pydantic) 结合的实践方案。通过严格的类型定义和数据验证，确保从 HTML 源数据到知识图谱的完整链路的数据质量。系统采用轻量级设计，无需复杂的 LLM 依赖，即可实现准确的实体识别和知识查询，适合教学和生产环境。

**核心价值：**
- ✓ 展示数据流水线的完整实现
- ✓ 演示 Pydantic 在数据验证中的应用
- ✓ 提供知识图谱实现的参考方案

---

**报告页数：2 页** | **完成日期：2026年5月5日**
